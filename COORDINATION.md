- 2026-09-11 worker (feat/copy-templates): ✅ **SHIPPED: the copy family's last vocabulary — and three hints that named the WRONG system (§3.143).** 📊 **722 → 725 playable as a SET DIFF, +3, ZERO lost** (`playable-set.mjs`, cached 2,100-card corpus, paired against the same `origin/main`): **Orthion, Hero of Lavabrink**, **Reflections of Littjara**, **The Jolly Balloon Man**. Gauntlet byte-identical — 97/320 = 30.3%, rows 17·14·19·7·8·10·17·5 — which is the right answer: the shipped pool is untouched, so a movement would have meant an accident. ⚠️ **THE AUDIT'S HEADLINE "21 CARDS" WAS NEVER 21, AND ANY AGENT PICKING WORK OFF A COVERAGE ROW SHOULD READ THIS.** A card joins an audit row if ANY of its unread clauses matches that row's hint PATTERN — so the copy row counted cards blocked four other ways too (Spree, Bestow, Class levels, Station, a d20, Ascend, `{P}` modes). Measured properly: of the 21, **exactly 3 had a copy clause as their only blocker**. The honest per-row number is `cards whose ONLY missing clause is this one`, and the audit does not print it. ✅ **WHAT LANDED.** (1) `"another target creature you control"` — the selector table refused it with a comment that had stopped being true, because `excludeSelf` already rides a TRIGGERED ability as `targetsExcludeSelf`. The ACTIVATED half needed the PAIR: `generateLegalActions` filters the source out of the menu AND `illegalTargetReasonForEffects` refuses it on the way in — one side alone leaves the engine accepting an action it never offered. `excludesSelfOfEffects` is the one answer both read; it is deliberately NOT inside `isLegalTarget`, which is answered from a DEFINITION (two Orthions share one and must still copy each other). (2) `"copy THAT spell"` — §3.31 reported this as "a trigger carries its triggering PLAYER but not the stack object", and half of that had stopped being true: `triggeringInstances` has carried "that creature" since §3.110. It is **one ROW** in `triggeringInstancesFor` (a `castSpell` condition that ASKS carries the cast spell's id) plus `subject: 'triggering'` on `copySpell`, which RE-READS the object off the stack rather than trusting the id. (3) an `"except"` tail carrying a base P/T, an ADDED colour (`CopyExceptions.addColors` — a union, not embalm's replacement), added types/subtypes and a keyword LIST. ⚠️ **THE SPLITTER WAS THE REAL BUG IN (3), AND IT IS A CLASS ANY TEMPLATE AUTHOR CAN HIT:** `splitExceptClauses` cut on every " and ", safe only while no accepted clause contained one — and "in addition to its other colors AND types" does. The tail tore into fragments matching nothing and the card reported a missing TEMPLATE when what was missing was the SPLIT. A separator is now only a separator where a clause BEGINS. ⚠️ **THE SUBJECT LIFT BROKE FORTY POOL CARDS ON ITS FIRST DRAFT, and the lesson generalises:** `absorb` flags a trigger's CONDITION when its BODY reads the subject — but the first version flagged exalted's and flanking's too, whose conditions supply the subject unconditionally and had never carried the field, breaking every byte-comparison the generated pool is pinned by. Which events make it opt-in is CORE's answer, now exported as `SUBJECT_OPT_IN_EVENTS` and read by the switch AND the lift. A second list in the compiler is exactly what went wrong. ⚠️ **THREE UNSUPPORTED_HINTS NAMED THE WRONG SYSTEM — one cost me an hour and would cost the next agent the same.** `explainUnsupported` returns the FIRST hint whose pattern matches the clause TEXT, and one printed sentence sits inside several templates. The DELAYED-removal hint claimed "this engine has no delayed triggered abilities" long after `createDelayedTrigger` shipped and `createTokenCopy` began compiling that exact sentence — so every Orthion-shaped card reported a missing SYSTEM while the real blocker was a selector three words away. Hints now take an optional **`when` guard**; Kitsa gets a new ACTIVATION-CONDITION row (its copy line is read in FULL — only "Activate only if ~'s power is 3 or greater" is not); and the quoted-ability hint now reaches Electroduplicate and Jaxis, saying WHY the two wordings differ (an "except" ability is copiable, a granted one is layer 6). `copy-effects.test.ts` gains a **TABLE of clause → the system its report must name**, so a hint that steals another's cards fails a test rather than someone's afternoon. 📏 **DELIBERATELY NOT BUILT, with the numbers that decided it.** `"~ becomes a copy of target …"` by an ACTIVATED ability (Mirage Mirror, Thespian's Stage, Mycosynth Gardens, Likeness Looter, Shifting Woodland, Silent Hallcreeper, Saheeli) looks like the biggest row left at **7 cards and is worth ONE**: the other six each carry a second blocker the system would not touch ("except it has THIS ABILITY", an X-scaled target, an activation condition, a "choose one that hasn't been chosen" modal, a loyalty ability). Only Mirage Mirror would go complete — for a new layer-1 effect that also needs a DURATION, which nothing in the engine yet un-applies at cleanup. An `"except"` tail on a SPELL copy (Fork) and a copy COUNT conditional on the cast-from zone (Increasing Vengeance) are both named in §3.31's residual and **neither card is in the 2,100-card corpus** — unmeasurable by this project's own instrument, so unbuilt. Six more one-row selectors (X count, a graveyard CARD, "that creature", "it", the four-way union, "from an enchantment source") each belong to a card ALSO blocked outside this family, so they buy 0 today; all are named in the hint for whoever lands their siblings. 🧪 **18 sabotages run, 16 RED — BOTH escapes were real holes in MY OWN tests.** (a) "copies nothing when the original was countered" cannot see a fallback to `stack[0]`, because by then the stack is EMPTY; a direct primitive test with a NON-empty stack and a dead id now pins it. (b) "refuses a keyword list with an unreadable word" used "flying and bushido 2", whose DIGIT fails the match one level earlier so the list branch was never reached; rewritten with "banding" plus a positive control. ✅ **GATE:** `npx vitest run` **21,820 passed / 0 failed** (401 files), `npm run verify` exit 0, `npm run build` exit 0. ⚠️ **POOL NOT REGENERATED** — the three names compile `'complete'` but `expanded-pool.ts` is generated from a gitignored scratch index this worktree does not have, and refreshing the card index is a network step. Left for the next `chore(data): regenerate the pool`, same as §3.139.
