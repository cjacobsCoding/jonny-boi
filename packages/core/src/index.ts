/**
 * Placeholder export proving the package builds and is importable across the
 * workspace. The real rules engine (GameState, event log, turn structure) lands
 * in roadmap item §3.1.
 */
export const PACKAGE_NAME = 'core';
