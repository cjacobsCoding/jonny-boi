{
  "name": "@jonny-boi/engine-representation-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PERF SPIKE (not shipped): separates DATA REPRESENTATION from LANGUAGE as causes of engine hot-path cost. Deliberately OUTSIDE the npm workspaces globs and the root vitest include so it cannot affect `npm run build` or `npm test`.",
  "scripts": {
    "build:wasm": "asc assembly/engine.ts --runtime stub --optimize --outFile build/engine.wasm --textFile build/engine.wat",
    "build:ts": "node build.mjs",
    "build": "npm run build:wasm && npm run build:ts",
    "verify": "node build/verify.js",
    "bench": "node build/bench.js",
    "calibrate": "node build/calibrate.js"
  },
  "devDependencies": {
    "assemblyscript": "0.27.37"
  }
}
